# Boca News Portal (frontend)

React + Vite app.

## Development

1) Ensure backend is running (expected: http://localhost:3001).
2) Configure env vars (see `.env.example`).
3) Install and run:

```bash
npm install
npm run dev
```

App runs on http://localhost:3000
